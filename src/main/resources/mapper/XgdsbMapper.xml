<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.als.tog.web.vlogs.mapper.XgdsbMapper">
    <select id="selectByType" parameterType="object" resultType="com.als.tog.web.vlogs.entity.Xgdsb">
        SELECT *
        FROM blogInfo
        WHERE CONCAT(',', REPLACE(tagTypes, ' ', ''), ',') LIKE concat('%', #{form.tagType}, '%')
        ORDER BY createTime DESC
    </select>
</mapper>